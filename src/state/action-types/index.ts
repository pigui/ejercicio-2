export enum MarkerTypes {
  ADD_MARKER = '[MarkerType] add_marker',
  DELETE_MARKER = '[MarkerType] delete_marker',
  ADD_MAP = '[MarkerType] add_map',
}

export enum SearchTypes {
  SEARCH = '[Search] search',
  SEARCH_SUCCESS = '[Search] search_succes',
}
